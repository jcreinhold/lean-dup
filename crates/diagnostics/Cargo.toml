[package]
categories = ["development-tools"]
description = "Progress, profiling, and structured errors for the lean-dup CLI."
keywords = ["lean", "lean4", "duplication", "audit"]
repository = "https://github.com/jcreinhold/lean-dup"
edition = { workspace = true }
license = { workspace = true }
name = "lean-dup-diagnostics"
version = { workspace = true }

[dependencies]
indicatif = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[lib]
doctest = false
