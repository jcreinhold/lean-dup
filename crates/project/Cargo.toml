[package]
categories = ["development-tools"]
description = "Lake workspace discovery and mathlib contract resolution for lean-dup."
keywords = ["lean", "lean4", "duplication", "audit"]
repository = "https://github.com/jcreinhold/lean-dup"
edition = { workspace = true }
license = { workspace = true }
name = "lean-dup-project"
version = { workspace = true }

[dependencies]
lean-dup-diagnostics = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }
walkdir = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lib]
doctest = false
