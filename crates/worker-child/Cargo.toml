[package]
build = "build.rs"
categories = ["development-tools"]
description = "Lean-linking worker-child host for lean-dup; loads the LeanDup capability through the lean-rs worker pool."
edition = { workspace = true }
keywords = ["audit", "duplication", "lean", "lean4"]
license = { workspace = true }
name = "lean-dup-worker-child"
repository = "https://github.com/jcreinhold/lean-dup"
version = { workspace = true }

[dependencies]
lean-rs-worker-child = { workspace = true }
tracing = { workspace = true }

[build-dependencies]
lean-toolchain = { workspace = true }

[[bin]]
name = "lean-dup-worker-child"
path = "src/main.rs"

[lints]
workspace = true
