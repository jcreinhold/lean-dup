[package]
categories = ["development-tools"]
description = "Lean worker process boundary for lean-dup: per-toolchain native worker resolution over the versioned worker protocol."
edition = { workspace = true }
keywords = ["audit", "duplication", "lean", "lean4"]
license = { workspace = true }
name = "lean-dup-worker"
repository = "https://github.com/jcreinhold/lean-dup"
version = { workspace = true }

[dependencies]
dirs = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
lean-dup-test-support = { workspace = true }
tempfile = { workspace = true }

[lib]
doctest = false
