# Rust build outputs.
target/

# lean-dup audit caches and Lake build outputs, including fixture workspaces.
**/.lean-dup/

# Lean build outputs.
**/.lake/
**/lake-manifest.json

# macOS metadata.
**/.DS_Store

# Python venv (Python implementation retired in e5b3baf; do not let it
# leak back into the dependency graph).
.venv/
__pycache__/

# Pi harness state
.pi/sessions/
